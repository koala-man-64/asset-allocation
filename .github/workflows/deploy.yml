name: Build and Deploy Tasks

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      force_redeploy:
        description: Force redeploy/update all jobs (ignore change filters)
        required: false
        type: boolean
        default: false

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read

env:
  FORCE_REDEPLOY: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.force_redeploy == 'true' }}
  ACR_NAME: assetallocationacr
  RESOURCE_GROUP: AssetAllocationRG
  # User-assigned managed identity used by Container Apps/Jobs to pull from ACR on first create.
  ACR_PULL_IDENTITY_NAME: asset-allocation-acr-pull-mi
  IMAGE_NAME: asset-allocation-scraper
  BRONZE_MARKET_JOB: bronze-market-job
  BRONZE_FINANCE_JOB: bronze-finance-job
  BRONZE_PRICE_TARGET_JOB: bronze-price-target-job
  BRONZE_EARNINGS_JOB: bronze-earnings-job
  GOLD_MARKET_JOB: gold-market-job
  GOLD_FINANCE_JOB: gold-finance-job
  GOLD_PRICE_TARGET_JOB: gold-price-target-job
  GOLD_EARNINGS_JOB: gold-earnings-job

  SILVER_MARKET_JOB: silver-market-job
  SILVER_FINANCE_JOB: silver-finance-job
  SILVER_PRICE_TARGET_JOB: silver-price-target-job
  SILVER_EARNINGS_JOB: silver-earnings-job
  
  # Standardized Container Names
  AZURE_CONTAINER_BRONZE: bronze
  AZURE_CONTAINER_SILVER: silver
  AZURE_CONTAINER_GOLD: gold
  AZURE_CONTAINER_PLATINUM: platinum
  AZURE_FOLDER_MARKET: market-data
  AZURE_FOLDER_FINANCE: finance-data
  AZURE_FOLDER_EARNINGS: earnings-data
  AZURE_FOLDER_TARGETS: price-target-data
  AZURE_CONTAINER_COMMON: common
  SERVICE_ACCOUNT_NAME: ${{ vars.SERVICE_ACCOUNT_NAME }}
  DEBUG_SYMBOLS: ${{ vars.DEBUG_SYMBOLS || '' }}

  # Alpha Vantage tunables (defaults match core/config.py)
  ALPHA_VANTAGE_RATE_LIMIT_PER_MIN: ${{ vars.ALPHA_VANTAGE_RATE_LIMIT_PER_MIN || '300' }}
  ALPHA_VANTAGE_TIMEOUT_SECONDS: ${{ vars.ALPHA_VANTAGE_TIMEOUT_SECONDS || '15' }}
  ALPHA_VANTAGE_MAX_WORKERS: ${{ vars.ALPHA_VANTAGE_MAX_WORKERS || '32' }}
  ALPHA_VANTAGE_EARNINGS_FRESH_DAYS: ${{ vars.ALPHA_VANTAGE_EARNINGS_FRESH_DAYS || '7' }}
  ALPHA_VANTAGE_FINANCE_FRESH_DAYS: ${{ vars.ALPHA_VANTAGE_FINANCE_FRESH_DAYS || '28' }}

jobs:
  tests:
    uses: ./.github/workflows/run_tests.yml
    secrets: inherit
    permissions:
      contents: read

  build-and-deploy:
    needs: tests
    if: github.ref == 'refs/heads/main'
    environment:
      name: production
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - name: Validate required secrets
        env:
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          ALPHA_VANTAGE_API_KEY: ${{ secrets.ALPHA_VANTAGE_API_KEY }}
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          SERVICE_ACCOUNT_NAME: ${{ vars.SERVICE_ACCOUNT_NAME }}
        run: |
          set -euo pipefail

          require_secret() {
            local name="$1"
            local value="${!name:-}"
            value="${value#"${value%%[![:space:]]*}"}"
            value="${value%"${value##*[![:space:]]}"}"
            if [ -z "$value" ]; then
              echo "::error::Missing required GitHub Actions secret: $name"
              exit 1
            fi
          }

          require_secret AZURE_CLIENT_ID
          require_secret AZURE_TENANT_ID
          require_secret AZURE_SUBSCRIPTION_ID
          require_secret AZURE_STORAGE_CONNECTION_STRING
          require_secret ALPHA_VANTAGE_API_KEY
          require_secret NASDAQ_API_KEY
          require_secret POSTGRES_DSN
          require_secret SERVICE_ACCOUNT_NAME

          python3 - <<'PY'
          import os
          import sys
          from urllib.parse import urlparse
          
          
          def parse_postgres_url(name: str) -> tuple[str, int, str]:
              raw = os.environ.get(name, "")
              value = raw.strip()
              parsed = urlparse(value)
              if parsed.scheme not in {"postgresql", "postgres"}:
                  print(f"::error::{name} must be a postgresql:// URL")
                  sys.exit(1)
              host = (parsed.hostname or "").strip().lower()
              if not host:
                  print(f"::error::{name} is missing host")
                  sys.exit(1)
              port = parsed.port or 5432
              db = (parsed.path or "").lstrip("/").strip()
              if not db:
                  print(f"::error::{name} is missing database name")
                  sys.exit(1)
              return host, int(port), db
          
          
          ranking = parse_postgres_url("POSTGRES_DSN")
          PY

      - name: Detect Market Changes
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3
        id: market_changes
        with:
          filters: |
            bronze_market:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "alpha_vantage/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/market_data/config.py"
              - "tasks/market_data/bronze_market_data.py"
              - "deploy/job_bronze_market_data.yaml"
              - ".github/workflows/deploy.yml"
            silver_market:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/market_data/config.py"
              - "tasks/market_data/silver_market_data.py"
              - "tasks/market_data/materialize_silver_market_by_date.py"
              - "deploy/job_silver_market_data.yaml"
              - ".github/workflows/deploy.yml"
            gold_market:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/market_data/config.py"
              - "tasks/market_data/gold_market_data.py"
              - "tasks/market_data/materialize_gold_market_by_date.py"
              - "deploy/job_gold_market_data.yaml"
              - ".github/workflows/deploy.yml"

      - name: Detect Finance Changes
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3
        id: finance_changes
        with:
          filters: |
            bronze_finance:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "alpha_vantage/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/finance_data/config.py"
              - "tasks/finance_data/bronze_finance_data.py"
              - "deploy/job_bronze_finance_data.yaml"
              - ".github/workflows/deploy.yml"
            silver_finance:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/finance_data/config.py"
              - "tasks/finance_data/silver_finance_data.py"
              - "tasks/finance_data/materialize_silver_finance_by_date.py"
              - "deploy/job_silver_finance_data.yaml"
              - ".github/workflows/deploy.yml"
            gold_finance:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/finance_data/config.py"
              - "tasks/finance_data/gold_finance_data.py"
              - "tasks/finance_data/materialize_gold_finance_by_date.py"
              - "deploy/job_gold_finance_data.yaml"
              - ".github/workflows/deploy.yml"

      - name: Detect Earnings Changes
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3
        id: earnings_changes
        with:
          filters: |
            bronze_earnings:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "alpha_vantage/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/earnings_data/config.py"
              - "tasks/earnings_data/bronze_earnings_data.py"
              - "deploy/job_bronze_earnings_data.yaml"
              - ".github/workflows/deploy.yml"
            silver_earnings:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/earnings_data/config.py"
              - "tasks/earnings_data/silver_earnings_data.py"
              - "tasks/earnings_data/materialize_silver_earnings_by_date.py"
              - "deploy/job_silver_earnings_data.yaml"
              - ".github/workflows/deploy.yml"
            gold_earnings:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/earnings_data/config.py"
              - "tasks/earnings_data/gold_earnings_data.py"
              - "tasks/earnings_data/materialize_gold_earnings_by_date.py"
              - "deploy/job_gold_earnings_data.yaml"
              - ".github/workflows/deploy.yml"

      - name: Detect Price Target Changes
        uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3
        id: price_target_changes
        with:
          filters: |
            bronze_price_target:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/price_target_data/config.py"
              - "tasks/price_target_data/bronze_price_target_data.py"
              - "deploy/job_bronze_price_target_data.yaml"
              - ".github/workflows/deploy.yml"
            silver_price_target:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/price_target_data/config.py"
              - "tasks/price_target_data/silver_price_target_data.py"
              - "tasks/price_target_data/materialize_silver_price_target_by_date.py"
              - "deploy/job_silver_price_target_data.yaml"
              - ".github/workflows/deploy.yml"
            gold_price_target:
              - "Dockerfile"
              - "pyproject.toml"
              - "requirements.lock.txt"
              - "core/**"
              - "services/**"
              - "alpaca/**"
              - "tasks/common/**"
              - "tasks/price_target_data/config.py"
              - "tasks/price_target_data/gold_price_target_data.py"
              - "tasks/price_target_data/materialize_gold_price_target_by_date.py"
              - "deploy/job_gold_price_target_data.yaml"
              - ".github/workflows/deploy.yml"

      - name: Log change detection outputs
        run: |
          echo "bronze_market=${{ steps.market_changes.outputs.bronze_market }}"
          echo "silver_market=${{ steps.market_changes.outputs.silver_market }}"
          echo "gold_market=${{ steps.market_changes.outputs.gold_market }}"
          echo "bronze_finance=${{ steps.finance_changes.outputs.bronze_finance }}"
          echo "silver_finance=${{ steps.finance_changes.outputs.silver_finance }}"
          echo "gold_finance=${{ steps.finance_changes.outputs.gold_finance }}"
          echo "bronze_earnings=${{ steps.earnings_changes.outputs.bronze_earnings }}"
          echo "silver_earnings=${{ steps.earnings_changes.outputs.silver_earnings }}"
          echo "gold_earnings=${{ steps.earnings_changes.outputs.gold_earnings }}"
          echo "bronze_price_target=${{ steps.price_target_changes.outputs.bronze_price_target }}"
          echo "silver_price_target=${{ steps.price_target_changes.outputs.silver_price_target }}"
          echo "gold_price_target=${{ steps.price_target_changes.outputs.gold_price_target }}"
      - name: Azure Login
        uses: azure/login@a457da9ea143d694b1b9c7c869ebb04ebe844ef5 # v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Ensure Azure Container Apps CLI (prefer built-in)
        run: |
          set -euo pipefail

          # Prefer the built-in `az containerapp ...` commands when available. The `containerapp`
          # extension can override built-in behavior (and has caused identity/YAML issues in the
          # past), so remove it first. If this runner's Azure CLI is too old, fall back to
          # installing the extension.
          az extension remove --name containerapp --only-show-errors > /dev/null 2>&1 || true

          if ! az containerapp -h > /dev/null 2>&1; then
            az extension add --name containerapp --upgrade --only-show-errors
          fi

          az version --query '"azure-cli"' -o tsv
          az extension show --name containerapp --query version -o tsv 2>/dev/null || echo "containerapp extension: not installed"

      - name: Ensure ACR Pull Identity Exists (User-Assigned MI)
        run: |
          set -euo pipefail

          identity_id="/subscriptions/${{ secrets.AZURE_SUBSCRIPTION_ID }}/resourceGroups/${{ env.RESOURCE_GROUP }}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/${{ env.ACR_PULL_IDENTITY_NAME }}"
          echo "ACR_PULL_IDENTITY_RESOURCE_ID=$identity_id" >> "$GITHUB_ENV"
          echo "Using ACR pull identity: $identity_id"

          if ! az identity show --ids "$identity_id" --only-show-errors > /dev/null 2>&1; then
            echo "::error::ACR pull identity '${{ env.ACR_PULL_IDENTITY_NAME }}' not found in '${{ env.RESOURCE_GROUP }}'. Run scripts/provision_azure.ps1 (with Owner or User Access Administrator) to create it and grant AcrPull."
            exit 1
          fi

          acr_id="$(az acr show --name "${{ env.ACR_NAME }}" --resource-group "${{ env.RESOURCE_GROUP }}" --query id -o tsv --only-show-errors | tr -d '\r')"
          principal_id="$(az identity show --ids "$identity_id" --query principalId -o tsv --only-show-errors | tr -d '\r')"
          client_id="$(az identity show --ids "$identity_id" --query clientId -o tsv --only-show-errors | tr -d '\r')"
          echo "ACR_PULL_IDENTITY_CLIENT_ID=$client_id" >> "$GITHUB_ENV"
          echo "Using ACR pull identity clientId: $client_id"

          # Ensure AcrPull on ACR for the UAMI used by Container Apps/Jobs.
          query="length([?roleDefinitionName=='AcrPull'])"
          existing="$(az role assignment list --assignee-object-id "$principal_id" --scope "$acr_id" --query "$query" -o tsv --only-show-errors 2>/dev/null | tr -d '\r' || true)"
          existing="${existing:-0}"
          if [ "$existing" = "0" ]; then
            echo "::error::AcrPull is not assigned for principalId=$principal_id. Run scripts/provision_azure.ps1 (with Owner or User Access Administrator) to grant AcrPull on the ACR."
            exit 1
          else
            echo "AcrPull already assigned for principalId=$principal_id."
          fi

      - name: Set Deployment Variables
        run: |
           echo "CONTAINER_APPS_ENVIRONMENT_ID=/subscriptions/${{ secrets.AZURE_SUBSCRIPTION_ID }}/resourceGroups/${{ env.RESOURCE_GROUP }}/providers/Microsoft.App/managedEnvironments/asset-allocation-env" >> "$GITHUB_ENV"

      - name: Log in to Azure Container Registry
        run: az acr login --name ${{ env.ACR_NAME }}

      - name: Generate Branch Tag
        id: branch_tag
        run: |
          # Sanitize branch name: replace non-alphanumeric chars with dashes
          # usage: feature/my-branch -> feature-my-branch
          RAW="${{ github.ref_name }}"
          SANITIZED="${RAW//[^a-zA-Z0-9]/-}"
          # Remove leading/trailing dashes if any created
          SANITIZED=$(echo "$SANITIZED" | sed 's/^-*//;s/-*$//')
          echo "tag=$SANITIZED" >> "$GITHUB_OUTPUT"

      - name: Build and Push Tasks Image
        run: |
          BRANCH_TAG="${{ steps.branch_tag.outputs.tag }}"
          
          # 1. Build immutable SHA tag
          docker build . -t ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
          
          # 2. Build branch tag
          docker build . -t ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:$BRANCH_TAG
          
          # 3. Push SHA tag and Branch tag
          docker push ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
          docker push ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:$BRANCH_TAG
          
          # 4. Handle 'latest' tag (Only on main branch)
          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
            echo "Pushing latest tag..."
            docker tag ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }} ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:latest
            docker push ${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:latest
          fi

      - name: Set JOB_IMAGE env
        run: |
          echo "JOB_IMAGE=${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}" >> "$GITHUB_ENV"

      - name: Ensure Storage Containers Exist
        run: |
          set -euo pipefail
          for container in bronze silver gold platinum; do
            az storage container create \
              --name $container \
              --account-name assetallocstorage001 \
              --auth-mode login
          done

      - name: Check Market Bronze Job Exists
        id: market_data_job_check
        run: |
          if az containerapp job show --name ${{ env.BRONZE_MARKET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Market Bronze Job check
        run: |
          echo "market_data_job_exists=${{ steps.market_data_job_check.outputs.job_exists }}"

      - name: Update and Start Market Bronze Job
        if: env.FORCE_REDEPLOY == 'true' || steps.market_changes.outputs.bronze_market == 'true' || steps.market_data_job_check.outputs.job_exists == 'false'
        env:
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          ALPHA_VANTAGE_API_KEY: ${{ secrets.ALPHA_VANTAGE_API_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Bronze Market Job from YAML..."
          tmp="deploy/job_bronze_market_data.tmp.yaml"
          envsubst < deploy/job_bronze_market_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.BRONZE_MARKET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.BRONZE_MARKET_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.BRONZE_MARKET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.BRONZE_MARKET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.BRONZE_MARKET_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.BRONZE_MARKET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi
          
          az containerapp job start \
            --name ${{ env.BRONZE_MARKET_JOB }} \
            --resource-group ${{ env.RESOURCE_GROUP }}

      - name: Check Market Silver Job Exists
        id: market_silver_job_check
        run: |
          if az containerapp job show --name ${{ env.SILVER_MARKET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Market Silver job check
        run: |
          echo "market_silver_job_exists=${{ steps.market_silver_job_check.outputs.job_exists }}"

      - name: Update Market Silver Job
        if: env.FORCE_REDEPLOY == 'true' || steps.market_changes.outputs.silver_market == 'true' || steps.market_silver_job_check.outputs.job_exists == 'false'
        env:
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Silver Market Job from YAML..."
          tmp="deploy/job_silver_market_data.tmp.yaml"
          envsubst < deploy/job_silver_market_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.SILVER_MARKET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.SILVER_MARKET_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.SILVER_MARKET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.SILVER_MARKET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.SILVER_MARKET_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.SILVER_MARKET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi

      - name: Check Market Gold Job Exists
        id: market_feature_job_check
        run: |
          if az containerapp job show --name ${{ env.GOLD_MARKET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Market Gold Job check
        run: |
          echo "market_feature_job_exists=${{ steps.market_feature_job_check.outputs.job_exists }}"
      - name: Update Market Gold Job
        if: env.FORCE_REDEPLOY == 'true' || steps.market_changes.outputs.gold_market == 'true' || steps.market_feature_job_check.outputs.job_exists == 'false'
        env:
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Gold Market Job from YAML..."
          tmp="deploy/job_gold_market_data.tmp.yaml"
          envsubst < deploy/job_gold_market_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.GOLD_MARKET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.GOLD_MARKET_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.GOLD_MARKET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.GOLD_MARKET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.GOLD_MARKET_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.GOLD_MARKET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi
      - name: Check Finance Bronze Job Exists
        id: finance_data_job_check
        run: |
          if az containerapp job show --name ${{ env.BRONZE_FINANCE_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Finance Bronze Job check
        run: |
          echo "finance_data_job_exists=${{ steps.finance_data_job_check.outputs.job_exists }}"
      - name: Update and Start Finance Bronze Job
        if: env.FORCE_REDEPLOY == 'true' || steps.finance_changes.outputs.bronze_finance == 'true' || steps.finance_data_job_check.outputs.job_exists == 'false'
        env:
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          ALPHA_VANTAGE_API_KEY: ${{ secrets.ALPHA_VANTAGE_API_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Bronze Finance Job from YAML..."
          tmp="deploy/job_bronze_finance_data.tmp.yaml"
          envsubst < deploy/job_bronze_finance_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.BRONZE_FINANCE_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.BRONZE_FINANCE_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.BRONZE_FINANCE_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.BRONZE_FINANCE_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.BRONZE_FINANCE_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.BRONZE_FINANCE_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi
          
          az containerapp job start \
            --name ${{ env.BRONZE_FINANCE_JOB }} \
            --resource-group ${{ env.RESOURCE_GROUP }}

      - name: Check Finance Silver Job Exists
        id: finance_silver_job_check
        run: |
          if az containerapp job show --name ${{ env.SILVER_FINANCE_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Finance Silver job check
        run: |
          echo "finance_silver_job_exists=${{ steps.finance_silver_job_check.outputs.job_exists }}"

      - name: Update Finance Silver Job
        if: env.FORCE_REDEPLOY == 'true' || steps.finance_changes.outputs.silver_finance == 'true' || steps.finance_silver_job_check.outputs.job_exists == 'false'
        env:
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Silver Finance Job from YAML..."
          tmp="deploy/job_silver_finance_data.tmp.yaml"
          envsubst < deploy/job_silver_finance_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.SILVER_FINANCE_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.SILVER_FINANCE_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.SILVER_FINANCE_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.SILVER_FINANCE_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.SILVER_FINANCE_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.SILVER_FINANCE_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi

      - name: Check Finance Gold Job Exists
        id: finance_feature_job_check
        run: |
          if az containerapp job show --name ${{ env.GOLD_FINANCE_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Finance Gold Job check
        run: |
          echo "finance_feature_job_exists=${{ steps.finance_feature_job_check.outputs.job_exists }}"
      - name: Update Finance Gold Job
        if: env.FORCE_REDEPLOY == 'true' || steps.finance_changes.outputs.gold_finance == 'true' || steps.finance_feature_job_check.outputs.job_exists == 'false'
        env:
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Gold Finance Job from YAML..."
          tmp="deploy/job_gold_finance_data.tmp.yaml"
          envsubst < deploy/job_gold_finance_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.GOLD_FINANCE_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.GOLD_FINANCE_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.GOLD_FINANCE_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.GOLD_FINANCE_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.GOLD_FINANCE_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.GOLD_FINANCE_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi
      
      - name: Check Price Target Job Exists
        id: price_target_job_check
        run: |
          if az containerapp job show --name ${{ env.BRONZE_PRICE_TARGET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Price Target job check
        run: |
          echo "price_target_job_exists=${{ steps.price_target_job_check.outputs.job_exists }}"
      - name: Update and Start Price Target Job
        if: env.FORCE_REDEPLOY == 'true' || steps.price_target_changes.outputs.bronze_price_target == 'true' || steps.price_target_job_check.outputs.job_exists == 'false'
        env:
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Bronze Price Target Job from YAML..."
          tmp="deploy/job_bronze_price_target_data.tmp.yaml"
          envsubst < deploy/job_bronze_price_target_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.BRONZE_PRICE_TARGET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.BRONZE_PRICE_TARGET_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.BRONZE_PRICE_TARGET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.BRONZE_PRICE_TARGET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.BRONZE_PRICE_TARGET_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.BRONZE_PRICE_TARGET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi
          
          az containerapp job start \
            --name ${{ env.BRONZE_PRICE_TARGET_JOB }} \
            --resource-group ${{ env.RESOURCE_GROUP }}

      - name: Check Price Target Silver Job Exists
        id: price_target_silver_job_check
        run: |
          if az containerapp job show --name ${{ env.SILVER_PRICE_TARGET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Price Target Silver job check
        run: |
          echo "price_target_silver_job_exists=${{ steps.price_target_silver_job_check.outputs.job_exists }}"

      - name: Update Price Target Silver Job
        if: env.FORCE_REDEPLOY == 'true' || steps.price_target_changes.outputs.silver_price_target == 'true' || steps.price_target_silver_job_check.outputs.job_exists == 'false'
        env:
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Silver Price Target Job from YAML..."
          tmp="deploy/job_silver_price_target_data.tmp.yaml"
          envsubst < deploy/job_silver_price_target_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.SILVER_PRICE_TARGET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.SILVER_PRICE_TARGET_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.SILVER_PRICE_TARGET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.SILVER_PRICE_TARGET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.SILVER_PRICE_TARGET_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.SILVER_PRICE_TARGET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi

      - name: Check Price Target Gold Job Exists
        id: price_target_feature_job_check
        run: |
          if az containerapp job show --name ${{ env.GOLD_PRICE_TARGET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Price Target Gold Job check
        run: |
          echo "price_target_feature_job_exists=${{ steps.price_target_feature_job_check.outputs.job_exists }}"
      - name: Update Price Target Gold Job
        if: env.FORCE_REDEPLOY == 'true' || steps.price_target_changes.outputs.gold_price_target == 'true' || steps.price_target_feature_job_check.outputs.job_exists == 'false'
        env:
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Gold Price Target Job from YAML..."
          tmp="deploy/job_gold_price_target_data.tmp.yaml"
          envsubst < deploy/job_gold_price_target_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.GOLD_PRICE_TARGET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.GOLD_PRICE_TARGET_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.GOLD_PRICE_TARGET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.GOLD_PRICE_TARGET_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.GOLD_PRICE_TARGET_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.GOLD_PRICE_TARGET_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi
      
      - name: Check Earnings Bronze Job Exists
        id: earnings_data_job_check
        run: |
          if az containerapp job show --name ${{ env.BRONZE_EARNINGS_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Earnings Bronze Job check
        run: |
          echo "earnings_data_job_exists=${{ steps.earnings_data_job_check.outputs.job_exists }}"
      - name: Update and Start Earnings Bronze Job
        if: env.FORCE_REDEPLOY == 'true' || steps.earnings_changes.outputs.bronze_earnings == 'true' || steps.earnings_data_job_check.outputs.job_exists == 'false'
        env:
          ALPHA_VANTAGE_API_KEY: ${{ secrets.ALPHA_VANTAGE_API_KEY }}
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Bronze Earnings Job from YAML..."
          tmp="deploy/job_bronze_earnings_data.tmp.yaml"
          envsubst < deploy/job_bronze_earnings_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.BRONZE_EARNINGS_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.BRONZE_EARNINGS_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.BRONZE_EARNINGS_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.BRONZE_EARNINGS_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.BRONZE_EARNINGS_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.BRONZE_EARNINGS_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi
          
          az containerapp job start \
            --name ${{ env.BRONZE_EARNINGS_JOB }} \
            --resource-group ${{ env.RESOURCE_GROUP }}

      - name: Check Earnings Silver Job Exists
        id: earnings_silver_job_check
        run: |
          if az containerapp job show --name ${{ env.SILVER_EARNINGS_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Earnings Silver job check
        run: |
          echo "earnings_silver_job_exists=${{ steps.earnings_silver_job_check.outputs.job_exists }}"

      - name: Update Earnings Silver Job
        if: env.FORCE_REDEPLOY == 'true' || steps.earnings_changes.outputs.silver_earnings == 'true' || steps.earnings_silver_job_check.outputs.job_exists == 'false'
        env:
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Silver Earnings Job from YAML..."
          tmp="deploy/job_silver_earnings_data.tmp.yaml"
          envsubst < deploy/job_silver_earnings_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.SILVER_EARNINGS_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.SILVER_EARNINGS_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.SILVER_EARNINGS_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.SILVER_EARNINGS_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.SILVER_EARNINGS_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.SILVER_EARNINGS_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi

      - name: Check Earnings Gold Job Exists
        id: earnings_feature_job_check
        run: |
          if az containerapp job show --name ${{ env.GOLD_EARNINGS_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "job_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "job_exists=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Log Earnings Gold Job check
        run: |
          echo "earnings_feature_job_exists=${{ steps.earnings_feature_job_check.outputs.job_exists }}"
      - name: Update Earnings Gold Job
        if: env.FORCE_REDEPLOY == 'true' || steps.earnings_changes.outputs.gold_earnings == 'true' || steps.earnings_feature_job_check.outputs.job_exists == 'false'
        env:
          AZURE_STORAGE_CONNECTION_STRING: ${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}
          NASDAQ_API_KEY: ${{ secrets.NASDAQ_API_KEY }}
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
          ACR_PULL_IDENTITY_RESOURCE_ID: ${{ env.ACR_PULL_IDENTITY_RESOURCE_ID }}
          CONTAINER_APPS_ENVIRONMENT_ID: ${{ env.CONTAINER_APPS_ENVIRONMENT_ID }}
        run: |
          set -euo pipefail
          : "${ACR_PULL_IDENTITY_RESOURCE_ID:?ACR_PULL_IDENTITY_RESOURCE_ID is required}"
          : "${JOB_IMAGE:?JOB_IMAGE is required}"

          echo "Deploying Gold Earnings Job from YAML..."
          tmp="deploy/job_gold_earnings_data.tmp.yaml"
          envsubst < deploy/job_gold_earnings_data.yaml > "$tmp"
          echo "Rendered YAML (secrets redacted):"
          awk '
            {
              line=$0
              match(line, /^ */)
              indent=RLENGTH
              if (match(line, /^[[:space:]]*secrets:[[:space:]]*$/)) { in_secrets=1; print line; next }
              if (in_secrets && indent <= 2 && match(line, /^[[:space:]]*[A-Za-z0-9_-]+:/)) { in_secrets=0 }
              if (in_secrets && match(line, /^[[:space:]]*value:/)) { sub(/value:.*/, "value: ***REDACTED***", line) }
              print line
            }
          ' "$tmp"
          
          if ! az containerapp job show --name ${{ env.GOLD_EARNINGS_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} > /dev/null 2>&1; then
            echo "Job ${{ env.GOLD_EARNINGS_JOB }} not found. Creating..."
            az containerapp job create \
              --name ${{ env.GOLD_EARNINGS_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --environment asset-allocation-env \
              --yaml "$tmp"
          else
            echo "Ensuring managed identity is assigned (for ACR pull)..."
            if ! az containerapp job identity show --name ${{ env.GOLD_EARNINGS_JOB }} --resource-group ${{ env.RESOURCE_GROUP }} --query userAssignedIdentities -o json 2>/dev/null | grep -q "$ACR_PULL_IDENTITY_RESOURCE_ID"; then
              az containerapp job identity assign \
                --name ${{ env.GOLD_EARNINGS_JOB }} \
                --resource-group ${{ env.RESOURCE_GROUP }} \
                --user-assigned "$ACR_PULL_IDENTITY_RESOURCE_ID" \
                --only-show-errors
            fi

            az containerapp job update \
              --name ${{ env.GOLD_EARNINGS_JOB }} \
              --resource-group ${{ env.RESOURCE_GROUP }} \
              --yaml "$tmp"
          fi
      
      - name: Delete Deprecated By-Date Jobs
        run: |
          set -euo pipefail

          delete_job_if_exists() {
            local job_name="$1"
            if az containerapp job show --name "$job_name" --resource-group "${{ env.RESOURCE_GROUP }}" > /dev/null 2>&1; then
              echo "Deleting deprecated job $job_name..."
              az containerapp job delete --name "$job_name" --resource-group "${{ env.RESOURCE_GROUP }}" --yes
            else
              echo "Deprecated job $job_name not found; skipping."
            fi
          }

          delete_job_if_exists "silver-market-by-date-job"
          delete_job_if_exists "silver-finance-by-date-job"
          delete_job_if_exists "silver-price-target-by-date-job"
          delete_job_if_exists "silver-earnings-by-date-job"
          delete_job_if_exists "gold-market-by-date-job"
          delete_job_if_exists "gold-finance-by-date-job"
          delete_job_if_exists "gold-price-target-by-date-job"
          delete_job_if_exists "gold-earnings-by-date-job"

      - name: Verify Updated Job Images
        run: |
          set -euo pipefail
          expected_image="${{ env.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}"

          check_job() {
            local job_name="$1"
            local actual_image
            actual_image=$(az containerapp job show --name "$job_name" --resource-group "${{ env.RESOURCE_GROUP }}" --query "properties.template.containers[0].image" -o tsv)
            if [ "$actual_image" != "$expected_image" ]; then
              echo "Image mismatch for $job_name: expected $expected_image, got $actual_image"
              exit 1
            fi
            echo "Verified $job_name image $actual_image"
          }

          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.market_changes.outputs.bronze_market }}" = "true" ] || [ "${{ steps.market_data_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.BRONZE_MARKET_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.market_changes.outputs.silver_market }}" = "true" ] || [ "${{ steps.market_silver_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.SILVER_MARKET_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.market_changes.outputs.gold_market }}" = "true" ] || [ "${{ steps.market_feature_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.GOLD_MARKET_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.finance_changes.outputs.bronze_finance }}" = "true" ] || [ "${{ steps.finance_data_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.BRONZE_FINANCE_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.finance_changes.outputs.silver_finance }}" = "true" ] || [ "${{ steps.finance_silver_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.SILVER_FINANCE_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.finance_changes.outputs.gold_finance }}" = "true" ] || [ "${{ steps.finance_feature_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.GOLD_FINANCE_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.price_target_changes.outputs.bronze_price_target }}" = "true" ] || [ "${{ steps.price_target_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.BRONZE_PRICE_TARGET_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.price_target_changes.outputs.silver_price_target }}" = "true" ] || [ "${{ steps.price_target_silver_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.SILVER_PRICE_TARGET_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.price_target_changes.outputs.gold_price_target }}" = "true" ] || [ "${{ steps.price_target_feature_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.GOLD_PRICE_TARGET_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.earnings_changes.outputs.bronze_earnings }}" = "true" ] || [ "${{ steps.earnings_data_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.BRONZE_EARNINGS_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.earnings_changes.outputs.silver_earnings }}" = "true" ] || [ "${{ steps.earnings_silver_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.SILVER_EARNINGS_JOB }}"
          fi
          if [ "${{ env.FORCE_REDEPLOY }}" = "true" ] || [ "${{ steps.earnings_changes.outputs.gold_earnings }}" = "true" ] || [ "${{ steps.earnings_feature_job_check.outputs.job_exists }}" = "false" ]; then
            check_job "${{ env.GOLD_EARNINGS_JOB }}"
          fi

      - name: Update and Start Feature Jobs Post-Deploy
        run: |
          echo "All feature jobs are started in their respective update steps."
